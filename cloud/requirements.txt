fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
itsdangerous>=2.1.0
python-multipart>=0.0.9
cryptography>=42.0.0
argon2-cffi>=23.1.0
boto3>=1.34.0
# Azure Blob storage backend for Arkive Cloud (low-cost Cool/Cold tiers).
azure-storage-blob>=12.19.0
httpx>=0.27.0
psycopg[binary]>=3.1.0
webauthn>=2.1.0,<3
# iCloud connector (unofficial API): enables Photos / Drive / Contacts sync.
pyicloud>=1.0.0
# Post-quantum primitives (ML-KEM / ML-DSA / SLH-DSA) via liboqs.
# Optional: the prototype runs without it using a clearly-flagged fallback.
# The Open Quantum Safe Python binding is `liboqs-python` (imports as `oqs`) and
# requires the native liboqs library. The PyPI package named `oqs` is UNRELATED.
# liboqs-python>=0.10.0
