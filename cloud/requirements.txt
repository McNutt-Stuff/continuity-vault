fastapi>=0.115.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
# pydantic 2.13+ is required by pyicloud 2.7.x; keep the FastAPI stack in step.
pydantic>=2.13.4,<3
pydantic-settings>=2.7.0
itsdangerous>=2.1.0
python-multipart>=0.0.9
# cryptography 44+ is required by pyicloud 2.7.x.
cryptography>=44.0.0
argon2-cffi>=23.1.0
boto3>=1.34.0
# Azure Blob storage backend for Arkive Cloud (low-cost Cool/Cold tiers).
azure-storage-blob>=12.19.0
# Azure infrastructure auto-provisioning (Hyperscaler Auto-Provision): create VM
# nodes, networking, and DNS records. Imported lazily — only used when an Azure
# hyperscaler service object runs a deploy.
azure-identity>=1.16.0
azure-mgmt-compute>=30.0.0
azure-mgmt-network>=25.0.0
azure-mgmt-dns>=8.1.0
# Google Cloud Storage backend for customer bring-your-own storage.
# 3.x+ (like pyicloud 2.7) is on protobuf 6 — pin the floor so pip upgrades the
# whole google stack onto a protobuf that also satisfies pyicloud (else the
# resolve conflicts and pip leaves pyicloud uninstalled).
google-cloud-storage>=3.0.0
# Google IAM Admin API (auto-provisioning scoped writer/reader service accounts).
google-api-python-client>=2.150.0
# Shared protobuf runtime for the google stack AND pyicloud (both need <8,>=6.x).
# Cap <7 so everyone stays on protobuf 6.x (what these wheels are generated for).
protobuf>=6.33.5,<7
httpx>=0.27.0
psycopg[binary]>=3.1.0
webauthn>=2.1.0,<3
# HEIC/HEIF decode for browser preview (Apple photos) — pillow-heif bundles
# libheif in its wheel, so no system package is needed.
Pillow>=11.0.0
pillow-heif>=1.0.0
# iCloud connector (unofficial API): enables Photos / Drive / Contacts sync.
# Pin the maintained 2.x (SRP) line — 1.x's auth flow is broken against current
# Apple, and 2.x delivers the 2FA code via api.request_2fa_code() (see live.py).
# 2.7.0+ is REQUIRED: 2.6.x can't complete Apple's HSA2 trusted-device BRIDGE, so
# both device-code and SMS verification are rejected. Its deps (protobuf>=6.32,
# pydantic>=2.13.4, cryptography>=44) are aligned with the google/FastAPI floors
# bumped above so pip resolves one consistent set. The [cli] extra is REQUIRED:
# `import pyicloud` transitively imports its Notes service, which imports `rich`
# at module load, but `rich`/`typer` ship only in the extras (packaging bug) — so
# the base install imports-errors ("No module named 'rich'") without [cli].
pyicloud[cli]>=2.7.0,<3
# Post-quantum primitives (ML-KEM / ML-DSA / SLH-DSA) via liboqs.
# Optional: the prototype runs without it using a clearly-flagged fallback.
# The Open Quantum Safe Python binding is `liboqs-python` (imports as `oqs`) and
# requires the native liboqs library. The PyPI package named `oqs` is UNRELATED.
# liboqs-python>=0.10.0
